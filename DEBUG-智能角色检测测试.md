# 智能角色检测测试指南

## 🎯 新功能：智能混合角色模式

系统现在会根据主题**自动判断**是使用固定角色模板还是动态创建角色。

---

## 📋 测试案例

### ✅ 测试1：动态角色（应该生成 @舞者 之类的标签）
**主题输入:** `一个女孩在跳舞`
**预期结果:**
- 第一步：生成正常剧本（无@标签）
- 第二步：AI自动创建 `@舞者 (22岁亚洲女孩，高马尾，黑色紧身舞蹈服)` 这样的标签

### ✅ 测试2：固定角色（应该使用 @小妹、@小哥）
**主题输入:** `小妹和小哥的日常生活`
**预期结果:**
- 第一步：生成正常剧本（无@标签，但用"女孩"、"男生"代指）
- 第二步：强制使用 `@小妹 (20岁中国女孩，黑色长直发...)`、`@小哥 (...)`

---

## 🔍 判断逻辑（关键词检测）

### 触发固定角色的关键词：
- `日常`、`生活`、`恋爱`、`约会`、`情侣`、`闺蜜`
- `小妹`、`小哥`
- `互动`、`对话`、`关系`、`相处`
- `早晨`、`晚上`、`回家`、`做饭`、`看电影`、`逛街`

### 其他主题 → 动态创建角色

---

## 🐛 调试日志

### 控制台日志输出：
1. 生成视频后会输出：`📹 视频URL: https://...`
2. 保存后会输出：`💾 已保存视频结果到 idea.result:`
3. 轮询时会输出：`📦 完整响应数据: {...}`

### 如何查看：
1. 右键网页 → 检查（F12）→ Console 标签
2. 找到上述带emoji的日志
3. 检查 `videoUrl` 是否正确获取

---

## 🎬 视频生成问题排查

如果前端没显示视频：
1. 检查控制台是否有 `📹 视频URL` 日志
2. 检查是否有 `❌ 任务成功但数据结构异常` 错误
3. 手动复制 `task_id`，在贞贞官网后台查看任务详情
4. 确认返回的 `data.data.output` 字段是否存在

---

## ✨ 期望效果

**动态角色模式:**
```
提示词1：@舞者 (22岁亚洲女孩，高马尾，黑色紧身舞蹈服，修长体型) 站在镜子前做热身动作...
```

**固定角色模式:**
```
提示词1：@小妹 (20岁中国女孩，黑色长直发空气刘海，身穿白色宽松衬衫) 坐在沙发上看书...
```

