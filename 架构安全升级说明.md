# 🔒 架构安全升级说明 v4.0

## 📋 本次更新内容

### ✅ 修复的安全问题

**之前（不安全）：**
- ❌ API Key 暴露在前端代码中
- ❌ 用户可通过浏览器查看源代码获取 Key
- ❌ Key 可被盗用到其他项目

**现在（安全）：**
- ✅ 所有 API Key 存储在后端服务器
- ✅ 前端只发送业务参数，不包含任何敏感信息
- ✅ 用户无法从浏览器获取 Key

---

## 🏗️ 新架构说明

### 前端（`js/batch.js`）

```javascript
// ✅ 无任何 API Key
// ✅ 只调用本地后端 /api/proxy
await fetch('/api/proxy', {
    method: 'POST',
    body: JSON.stringify({
        targetUrl: 'https://api.example.com',
        apiType: 'zhenzhen', // 告诉后端使用哪个 API
        body: { /* 业务参数 */ }
    })
});
```

### 后端（`启动服务器.js`）

```javascript
// 安全存储所有 API Key
const API_KEYS = {
    zhenzhen: 'sk-REDACTED', // 贞贞工坊
    rhFlux: {              // RH Flux
        apiKey: 'aaa...',
        webappId: 'xxx...'
    }
};

// 根据 apiType 自动添加认证
if (apiType === 'zhenzhen') {
    headers['Authorization'] = `Bearer ${API_KEYS.zhenzhen}`;
} else if (apiType === 'rh-flux') {
    body.apiKey = API_KEYS.rhFlux.apiKey;
    body.webappId = API_KEYS.rhFlux.webappId;
}
```

---

## 🎯 支持的 API 类型

| apiType | 说明 | 认证方式 |
|---------|------|----------|
| `zhenzhen` | 贞贞工坊（Grok-4, Sora2） | Header: Authorization |
| `rh-flux` | RH Flux 文生图 | Body: apiKey + webappId |
| `rh-flux-poll` | RH Flux 轮询任务 | Header: Authorization |
| `auto` | 自动识别（根据域名） | 自动判断 |

---

## 🚀 使用方法

### 1. 启动本地服务器

```bash
双击 🚀一键启动.bat
```

### 2. 访问页面

```
http://localhost:8899
```

### 3. 直接使用

- ✅ 无需配置任何 API Key
- ✅ 打开即用
- ✅ 所有认证由后端自动处理

---

## 🔐 安全性说明

### ✅ 保护的内容

1. **Zhenzhen API Key**: `sk-EXAMPLE` (示例，已移除真实 Key)
2. **RH Flux API Key**: `a380bfb6f25b4733ad6756a0bb0a8403`
3. **RH Flux webappId**: `1986431735514726401`

### ⚠️ 重要提醒

- 🔒 **后端文件（`启动服务器.js`）不要公开到 GitHub**
- 🔒 **如需开源，将 API Key 改为环境变量**
- 🔒 **定期更换 API Key 以提高安全性**

---

## 📊 架构对比

### 旧架构（v3.0）

```
用户浏览器 → 直接调用外部 API (带 Key) ❌
```

### 新架构（v4.0）

```
用户浏览器 → 本地后端 → 外部 API ✅
                ↑
            添加 Key
```

---

## 🛠️ 技术细节

### 代理实现

- **端口**: 8899
- **路由**: `/api/proxy`
- **方法**: POST（创建）/ GET（轮询）
- **协议**: HTTPS with SNI support

### 错误处理

- ✅ 详细的控制台日志
- ✅ 友好的错误提示
- ✅ 自动重试机制（轮询）

---

## 📝 升级日志

### v4.0 (2025-01-17)
- 🔒 API Key 移至后端
- 🔧 支持多 API 类型识别
- 🚀 优化代理性能

### v3.0 (之前)
- ⚠️ API Key 暴露在前端（不安全）

---

## 💡 后续建议

1. **环境变量化**：将 API Key 改为从 `.env` 文件读取
2. **部署到云端**：使用 Vercel Serverless 或其他云服务
3. **添加速率限制**：防止 API 滥用
4. **日志记录**：记录所有 API 调用以便审计

---

**🎉 现在你的项目已经是安全的前后端分离架构了！**

