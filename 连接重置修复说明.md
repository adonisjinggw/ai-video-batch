# 🔧 连接重置(ECONNRESET)问题修复说明

> **修复时间：** 2025-11-17  
> **问题：** `socket hang up` / `ECONNRESET` 错误  
> **影响范围：** T8星辰/贞贞工坊文本生成API

---

## 🔍 问题描述

在调用 T8星辰API (`https://ai.t8star.cn/v1/chat/completions`) 时，出现连接被服务器重置的错误：

```
❌ API请求失败: socket hang up
❌ 错误详情: ECONNRESET
```

---

## 📋 错误原因分析

### 可能的原因：
1. **缺少必要的请求头** - API服务器可能检测为非法请求
2. **SSL/TLS握手问题** - 协议版本不兼容
3. **API限流** - 请求频率过高被服务器拒绝
4. **超时设置不当** - 没有明确的超时时间
5. **网络不稳定** - 间歇性连接问题

---

## ✅ 修复方案

### 1. 增强HTTP请求头（后端）

**文件：** `ai-video-batch/启动服务器.js`

**修改位置：** 第69-77行

**修改内容：**
```javascript
// 准备请求头（增强兼容性）
const headers = {
    'Content-Type': 'application/json',
    'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36',
    'Accept': 'application/json, text/plain, */*',
    'Accept-Encoding': 'gzip, deflate, br',
    'Connection': 'keep-alive',
    'Cache-Control': 'no-cache'
};
```

**作用：**
- ✅ 添加 `User-Agent` 模拟浏览器请求
- ✅ 添加 `Accept` 明确接受的内容类型
- ✅ 添加 `Accept-Encoding` 支持压缩
- ✅ 添加 `Connection: keep-alive` 保持连接
- ✅ 添加 `Cache-Control` 避免缓存问题

---

### 2. 设置请求超时（后端）

**文件：** `ai-video-batch/启动服务器.js`

**修改位置：** 第103-116行

**修改内容：**
```javascript
const options = {
    hostname: targetUrl.hostname,
    port: 443,
    path: targetUrl.pathname + (targetUrl.search || ''),
    method: requestMethod,
    headers: headers,
    servername: targetUrl.hostname,  // 🔑 SNI支持
    timeout: 60000, // ⏱️ 60秒超时
    // 🔧 完整的TLS配置
    minVersion: 'TLSv1.2',
    maxVersion: 'TLSv1.3',
    ciphers: 'HIGH:!aNULL:!MD5',
    honorCipherOrder: true
};
```

**作用：**
- ✅ 设置60秒超时时间
- ✅ 避免无限等待

---

### 3. 增强错误处理（后端）

**文件：** `ai-video-batch/启动服务器.js`

**修改位置：** 第134-165行

**新增内容：**

#### **超时处理：**
```javascript
proxyReq.on('timeout', () => {
    console.error('⏱️ API请求超时');
    proxyReq.destroy();
    res.writeHead(504, { 'Content-Type': 'application/json; charset=utf-8' });
    res.end(JSON.stringify({ error: 'API请求超时，请稍后重试' }));
});
```

#### **错误分类处理：**
```javascript
proxyReq.on('error', (error) => {
    console.error('❌ API请求失败:', error.message);
    console.error('❌ 错误代码:', error.code || 'UNKNOWN');
    console.error('❌ 目标地址:', targetUrl.href);
    
    let errorMessage = error.message;
    if (error.code === 'ECONNRESET') {
        errorMessage = 'API连接被重置，可能是网络问题或API限流，请稍后重试';
    } else if (error.code === 'ETIMEDOUT') {
        errorMessage = 'API请求超时，请检查网络连接';
    } else if (error.code === 'ENOTFOUND') {
        errorMessage = 'API地址无法解析，请检查API配置';
    }
    
    res.writeHead(500, { 
        'Content-Type': 'application/json; charset=utf-8',
        'Access-Control-Allow-Origin': '*'
    });
    res.end(JSON.stringify({ 
        error: errorMessage,
        code: error.code 
    }));
});
```

**作用：**
- ✅ 区分不同的错误类型
- ✅ 提供更友好的错误提示
- ✅ 记录详细的调试信息

---

### 4. 前端自动重试机制

**文件：** `ai-video-batch/js/batch.js`

**修改位置：** 第501-554行

**修改内容：**
```javascript
async function callZhenzhenTextAPI(prompt, retryCount = 0) {
    const maxRetries = 2; // 最多重试2次
    
    try {
        console.log(`📝 调用T8文本生成API${retryCount > 0 ? ` (重试 ${retryCount}/${maxRetries})` : ''}`);
        
        // ... API调用代码 ...
        
    } catch (error) {
        console.error(`❌ 文本API调用失败 (尝试 ${retryCount + 1}/${maxRetries + 1}):`, error.message);
        
        // 如果是连接重置错误且还有重试次数，则重试
        if ((error.message.includes('被重置') || error.message.includes('ECONNRESET') || error.message.includes('连接')) && retryCount < maxRetries) {
            console.log(`⏳ 等待3秒后重试...`);
            await new Promise(resolve => setTimeout(resolve, 3000));
            return callZhenzhenTextAPI(prompt, retryCount + 1);
        }
        
        throw error;
    }
}
```

**作用：**
- ✅ 自动重试2次（共3次尝试）
- ✅ 每次重试间隔3秒
- ✅ 只对连接相关错误重试
- ✅ 显示重试进度

---

## 🎯 修复效果

### 修复前：
```
❌ API请求失败: socket hang up
❌ 错误详情: ECONNRESET
```

### 修复后：

#### **情况1：首次请求成功**
```
📝 调用T8文本生成API
✅ T8文本生成成功
```

#### **情况2：首次失败，重试成功**
```
📝 调用T8文本生成API
❌ 文本API调用失败 (尝试 1/3): API连接被重置...
⏳ 等待3秒后重试...
📝 调用T8文本生成API (重试 1/2)
✅ T8文本生成成功
```

#### **情况3：多次失败后提示**
```
📝 调用T8文本生成API
❌ 文本API调用失败 (尝试 1/3): API连接被重置...
⏳ 等待3秒后重试...
📝 调用T8文本生成API (重试 1/2)
❌ 文本API调用失败 (尝试 2/3): API连接被重置...
⏳ 等待3秒后重试...
📝 调用T8文本生成API (重试 2/2)
❌ 文本API调用失败 (尝试 3/3): API连接被重置，可能是网络问题或API限流，请稍后重试
```

---

## 🧪 测试步骤

### 1. 重启服务器

```bash
cd ai-video-batch
# 关闭旧服务器（Ctrl+C）
启动服务器.bat
```

### 2. 打开浏览器测试

```
http://localhost:3000
```

### 3. 测试文生图+图生视频模式

1. 输入创意描述
2. 点击"文生图+图生视频"按钮
3. 打开控制台（F12）查看日志

### 4. 观察日志

**正常情况：**
```
📝 调用T8文本生成API
✅ 响应状态: 200
✅ T8文本生成成功
```

**重试情况：**
```
📝 调用T8文本生成API
❌ 文本API调用失败 (尝试 1/3)
⏳ 等待3秒后重试...
✅ T8文本生成成功
```

---

## ⚠️ 注意事项

1. **API限流：** 如果频繁失败，可能是API限流，建议降低请求频率
2. **网络质量：** 确保网络连接稳定
3. **API配额：** 检查T8星辰API的剩余配额
4. **API Key：** 确保API Key有效且未过期

---

## 📋 修改文件清单

| 文件 | 修改内容 | 作用 |
|------|---------|------|
| `启动服务器.js` | 增强请求头 | 模拟浏览器请求，提高兼容性 |
| `启动服务器.js` | 设置超时时间 | 避免无限等待 |
| `启动服务器.js` | 增强错误处理 | 区分错误类型，友好提示 |
| `js/batch.js` | 自动重试机制 | 提高成功率 |

---

## 🎉 完成状态

- ✅ 后端请求头增强
- ✅ 后端超时设置
- ✅ 后端错误处理优化
- ✅ 前端自动重试机制
- ✅ 错误日志完善

**修复完成！请重启服务器测试！** 🚀

