# AI 视频批量创作 - 云端发布说明（语音控制 + 无 Key 暴露）

## 1. 新增功能概览
- 🎙️ **语音输入**：左侧创意输入区新增“语音输入”按钮，调用浏览器 Web Speech API（支持中文普通话），适合不会打字的小朋友。
- 🌐 **自动代理切换**：
  - 本地通过 `启动服务器.js` → `http://localhost:8899/api/proxy`
  - 线上自动切换到 `https://nano-video-proxy.vercel.app/api/proxy`
- 🗂️ **Vercel 云端代理**：新增 `ai-video-batch/vercel/api/proxy.js`（Serverless Function）+ `vercel.json`，避免在前端暴露任何 API Key。

## 2. 部署后端（Vercel）
1. 安装 Vercel CLI：`npm i -g vercel`
2. 进入 `ai-video-batch` 目录
3. 设置环境变量：
   - `T8STAR_API_KEY`
   - `RH_FLUX_API_KEY`
   - `RH_FLUX_WEBAPP_ID`
4. 运行 `vercel --prod`
5. 记录生成的域名（本说明默认 `https://nano-video-proxy.vercel.app`）

> 如需更换域名，请在 `js/batch.js` 中修改 `REMOTE_PROXY_ENDPOINT`。

## 3. 部署前端（PinMe 或任意静态空间）
1. 将 `ai-video-batch` 目录（除 `启动服务器.js`、`vercel/` 等本地文件）打包上传到 PinMe
2. 前端会自动检测运行环境：
   - `file://` 或公网域名 → 直接调用 Vercel 代理
   - `localhost:8899` → 调用本地代理，兼容线下演示

## 4. 使用语音输入的注意事项
- 仅支持 HTTPS、localhost 或 file 协议（浏览器安全策略）
- 建议使用 Chrome / Edge / 夸克等 Chromium 内核浏览器
- 如果浏览器不支持，会提示“当前浏览器不支持语音输入”

## 5. 自定义代理端点
- 在浏览器控制台执行：
  ```js
  localStorage.setItem('customProxyEndpoint', 'https://your-proxy.com/api/proxy');
  ```
- 清除自定义值：
  ```js
  localStorage.removeItem('customProxyEndpoint');
  ```

## 6. 本地调试
1. 双击 `🚀一键启动.bat`（会启动静态文件 + 本地代理）
2. 打开 `http://localhost:8899`
3. 所有请求仍然走本地 `启动服务器.js`，与线上逻辑保持一致

---
现在可以做到：
- ✅ 打开网页即用（PinMe/静态托管）
- ✅ Key 永不暴露（全部在 Vercel 环境变量中）
- ✅ 小朋友可用语音说创意

