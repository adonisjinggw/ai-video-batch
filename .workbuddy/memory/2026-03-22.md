# 2026-03-22 工作日志

## writing.html 恢复与部署
- 本地 writing.html 被错误覆盖为 _clean_deploy 旧版（2612行），丢失32次git提交的功能
- 通过 `git checkout HEAD -- writing.html` 从 git 恢复完整版（3079行），包含短剧+长篇+响应式布局
- 短剧面板 UI 完整：原创/改编、主题输入、14种类型、风格、集数、模型选择
- 长篇面板 UI 完整：项目管理、题材（23种）、章节数、字数、模型

## short-drama.js 大纲解析正则修复
- 原正则 `第\s*(\d+)\s*集[：:\s]*(.+?)\s*[-—–]\s*(.+)` 改为 `第\s*(\d+)\s*集[：:：]\s*(.+?)\s*[-—–]\s*(.+)`
- 支持全角冒号（：）

## novel-engine.js 评分系统修复
- **bug**: `novelEvaluateAll` 评估后没把结果存到 `ch._evaluation`，导致 `novelBatchFixLowScoreChapters` 读不到评分，批量修正按钮点击无反应
- **修复**: 评估循环中添加 `novelState.chapters[i]._evaluation = result`
- **新增**: 评分面板每章添加手动输入分数框 + 修正按钮（所有章节可见）+ 重写按钮
- 新增 `novelManualSetScore()` 和 `novelRegenerateChapter()` 函数

## short-drama.js + writing.html 修复（浏览器测试发现）
- **bug**: writing.html 第2830行 `userId: currentUser.id` 没用可选链，未登录时崩溃
- **修复**: 改为 `userId: currentUser?.id`
- **新增**: short-drama.js 原创短剧和小说改编都加了登录检查
- 浏览器验证：未登录点击生成 → toast 提示正常
