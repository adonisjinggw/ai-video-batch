# 2026-04-03 工作日志

## Seedance 2.0 全面对齐修复 ✅ 已完成

### 后端修复（api/yunwu.js）

**1. 提交接口格式修复（403 → 200）**
- URL: `/openapi/v2/run/ai-app/${APP_ID}` → **`/task/openapi/ai-app/run`**
- Body: `{ nodeInfoList, instanceType }` → **`{ webappId, apiKey, nodeInfoList }`**
- 来源：RunningHub 官方 Apifox 文档

**2. 轮询接口统一（新增 Seedance 分支）**
- 发现 `poll` action 有**两套重复代码块**（3129行 + 3820行）
- 第一套已有 `_source === 'seedance'` 识别，但用的旧 `/openapi/v2/query`
- **修复**：第一套拆分为 LTX（`/openapi/v2/query`）和 Seedance（`/task/openapi/outputs`）两条分支
- 删除第二套里重复的 Seedance 代码
- Seedance 轮询流程：`POST /task/openapi/outputs` → `{ data: [{ fileUrl, fileType }] }` → 找 mp4/webm/mov

### 前端修复（video-tools.html）

**1. Ref 面板（视频克隆）缺少 Seedance 支持**
- `generateRef()` 的 `cloneMode === 'frame'` 分支：添加 Seedance → seedance-i2v
- `cloneMode === 'firstlast'` 分支：添加 Seedance → seedance-i2v（带 lastFrameImage）
- `cloneMode === 'ai'` 分支：添加 Seedance → seedance-t2v
- **轮询 useYunwu 缺少 isSeedance**：已补充

**2. Veo size 格式修复**
- `16x9` / `9x16` / `1x1`（比例）→ `1280x720` / `720x1280` / `720x720`（像素尺寸）
- seconds 最低强制 8 秒
- Veo 走直接 `fetch()` 不走 `_tryAllEndpoints`（避免URL拼接错误）

### 三种模式完整链路

| 模式 | Action | 前端请求 | 后端提交 | 返回 _source | 轮询 |
|------|--------|---------|---------|-------------|------|
| 文生视频 | seedance-t2v | prompt+ratio+duration | RunningHub AI App T2V | seedance | /task/openapi/outputs |
| 图生视频 | seedance-i2v | firstFrameImage(+lastFrame) | RunningHub AI App I2V | seedance | /task/openapi/outputs |
| 全能参考 | seedance-ref | refImage1/2/3 | RunningHub AI App Ref | seedance | /task/openapi/outputs |

### 同时修复前端 video-tools.html
- T2V/I2V/Batch 的 Seedance 请求体字段对齐后端：
  - `aspect_ratio` → `ratio`
  - 添加 `isLv10: true`, `skipBilling: true`, `resolution: '720p'`
手机版 skill-presets.js 的 VIDEO_MODEL_OPTIONS 缺少 Seedance 三个选项，导致技能系统/批量功能里看不到 Seedance 入口。

### 修复内容

**1. skill-presets.js**
- VIDEO_MODEL_OPTIONS 添加 Seedance 三个选项：
  - `seedance-t2v` - 文生视频 (8胶片/LV10+)
  - `seedance-i2v` - 图生视频 (10胶片/LV10+)
  - `seedance-ref` - 全能参考视频 (12胶片/LV10+)

**2. api-core.js**
- `__normalizeVideoModelName()` 添加 seedance 前缀保留
- `__getFixedClipDurationByModel()` 添加 seedance 默认5秒时长
- `callSora2TextToVideoAPI()` 添加 seedance-t2v 路由分支
- `callSora2ImageToVideoAPI()` 添加 seedance-i2v 和 seedance-ref 路由分支

**3. mobile.html**
- `VIDEO_COST_MAP` 添加 seedance 三个模型的扣费 (8/10/12胶片)
- `getVideoModelForCost()` 添加 seedance 模型识别
- 批量生成扣费映射添加 seedance 选项
- **修复 `promptText is not defined` 错误**：将 Seedance 代码从 switch default 分支移到独立 case 分支，避免访问未定义变量

**4. video-tools.html**
- 文生视频、图生视频、批量生成三个下拉框添加 Seedance 选项
- VIDEO_COST 添加 seedance 扣费映射
- **新增 `isSeedanceModel()` 判断函数**
- `generateT2V()` 添加 seedance 分支 → 走 `/api/yunwu` + action=`seedance-t2v`
- `generateI2V()` 添加 seedance-i2v / seedance-ref 分支
- `generateBatch()` T2V/I2V 都添加 seedance 分支
- 所有函数的 `useYunwu` 判断包含 `isSeedance`

### 部署状态
- ✅ 已部署到 lossloop.cn（多次迭代）
- ✅ 手机版技能面板、批量生成、快速生成均可使用 Seedance 2.0
- ✅ video-tools.html 视频工具页面已添加 Seedance 选项并修复路由
- ✅ 修复 promptText 未定义错误（mobile.html switch case 重构）
- ✅ 修复 video-tools.html 路由错误（seedance 误走 /api/sora2 → 改为 /api/yunwu）
- 扣费：T2V 8胶片 / I2V 10胶片 / Ref 12胶片
- 限制：仅 LV10+ 用户可用（后端校验）

## Veo 视频模型格式修复（十万火急）✅

### 问题
Veo 走 `/v1/chat/completions`（chat 模式）严重报错无法使用

### 修复
- **yunwu.js**：Veo 全部改为 OpenAI `/v1/videos` 格式
  - 端点：`POST https://yunwu.ai/v1/videos`
  - 格式：multipart/form-data（不是 JSON）
  - 字段：model, prompt, seconds, size, input_reference(垫图), watermark
  - 响应：{ id, object: "video", model, status, progress }
  - 已部署

## Seedance RunningHub API 格式修复（403 → 正确格式）

### 根因
代码用了错误的 URL 和请求体：
- ❌ 错误 URL: `/openapi/v2/run/ai-app/${APP_ID}`（这是旧版/不存在的接口）
- ❌ 错误 body: `{ nodeInfoList, instanceType, usePersonalQueue }`

### 修复为 RunningHub 官方文档格式
- ✅ 正确 URL: `POST https://www.runninghub.cn/task/openapi/ai-app/run`
- ✅ 正确 body: `{ webappId, apiKey, nodeInfoList }`
- 三个 Seedance 接口全部修复：T2V / I2V / Ref
- 来源：RunningHub Apifox 文档 "发起AI应用任务"
- 已部署到 lossloop.cn

### 同时修复前端 video-tools.html
- T2V/I2V/Batch 的 Seedance 请求体字段对齐后端：
  - `aspect_ratio` → `ratio`
  - 添加 `isLv10: true`, `skipBilling: true`, `resolution: '720p'`
