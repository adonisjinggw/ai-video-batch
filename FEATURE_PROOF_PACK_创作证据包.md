# 功能规划｜Proof Pack（创作证据包 / 可验真证据库）

## 1. 背景与目标

### 1.1 背景
当前 AI 视频/漫画/写作类平台同质化严重，主要竞争集中在：
- 模型更多
- 速度更快
- 参数更多

但真实商用场景里，用户（尤其是自媒体、工作室、企业客户）对“交付可信度 / 作品版本一致性 / 纠纷证据链”的需求越来越高：
- 作品被二次编辑后无法证明“原始生成版本”
- 客户验收需要可追溯（证明用的模型/参数/时间）
- 平台缺少可审计的创作记录，无法成为“可信交付方”

### 1.2 目标
为每一次生成提供一个**可验证、可导出、可复核**的证据链能力：
- 用户可一键导出“证据包”（Proof Pack）
- 任意第三方可在离线环境验证作品与证据包是否匹配、证据包是否被篡改
- 在不显著增加服务器成本的前提下，实现可商用落地的“可信交付”差异化能力

### 1.3 关键约束
- 不新增高成本推理
- 不新增高成本视频转码/渲染
- 证据包尽可能由前端生成与验证（低服务器压力）
- 默认不泄露提示词与隐私信息（可控导出）

---

## 2. 功能定义

### 2.1 概念
**Proof Pack（创作证据包）**：围绕某个任务/项目的生成结果，输出一个包含“参数快照 + 输出哈希清单 + 流程快照 + 签名/时间戳”的证据文件集合。

### 2.2 主要能力（对用户）
- **导出证据包**：对单个任务结果/单个节点结果/整个项目（画布）一键导出
- **验证作品**：上传作品文件 + 证据包，即可验证是否为该证据包对应版本（且未被篡改）
- **可控隐私导出**：默认仅导出提示词摘要，可选择导出完整提示词（可加密）

### 2.3 主要能力（对平台）
- **可信交付**：为企业/客户提供验真能力（交付闭环）
- **审计留存**：将关键生成信息结构化留存（可支持后续风控/纠纷处理）
- **商业化入口**：时间戳/签名/企业审计等增强功能可收费

---

## 3. 典型使用场景

- **工作室交付**：交付视频给客户，附带 Proof Pack。客户可验证“未被替换/未被篡改”。
- **版权纠纷**：创作者需要证明“某时间点由平台生成”。Proof Pack 提供证据链。
- **团队协作**：多人共同生产，需要明确每次输出的版本与参数。

---

## 4. MVP 方案（低成本可落地）

### 4.1 MVP 范围
MVP 只做 3 件事：
1) 生成 `manifest.json`（生成参数快照 + 输出哈希清单 + 流程快照）
2) 下载 Proof Pack（json 或 zip）
3) 验证页面（纯前端计算 hash 对比）

### 4.2 MVP 不做（明确延期）
- 第三方公证/时间戳（后续付费增强）
- 复杂的权限分享/访问日志（企业版增强）
- 端到端加密托管（后续增强）

---

## 5. 数据结构设计（建议）

### 5.1 Proof Pack 文件结构
建议导出为 zip（用户体验更完整），内部结构：
- `manifest.json`（必选）
- `README.txt`（可选：说明如何验证）
- `outputs/`（可选：是否将输出文件一并打包。默认不打包，仅记录URL+hash）

> 说明：如果输出文件体积大（视频），默认不放进 zip，以避免带宽/存储压力。Proof Pack 主要是“证据”，不是“交付拷贝”。

### 5.2 manifest.json 字段（MVP）
- **meta**
  - `schemaVersion`：如 `1.0`
  - `platform`：站点/产品标识
  - `appVersion`：前端版本号
  - `createdAt`：生成时间戳
  - `timezone`：如 `Asia/Shanghai`
- **owner（可脱敏）**
  - `userIdHash`：用户ID的hash（避免直接暴露）
  - `displayName`：可选
- **source（生成来源）**
  - `productType`：video/comic/writing
  - `taskId`：任务ID
  - `projectId`：可选（写作项目/协作项目）
  - `nodeId`：可选（若按节点导出）
- **generationSnapshot（生成参数快照）**
  - `model`：如 `sora-2` / `veo3`
  - `modelVersion`：可选
  - `settings`：关键参数（分镜数、比例、时长、风格、语言等）
  - `promptDigest`：提示词摘要（hash/截断）
  - `promptFull`：可选，默认不导出
- **workflowSnapshot（流程快照）**
  - `canvasSnapshot`：画布节点/连线序列化（可选，但推荐）
- **outputs（输出清单）**
  - 数组，每项：
    - `type`：image/video/text
    - `fileName`：建议名
    - `url`：可选
    - `sha256`：hash
    - `generatedAt`
    - `relatedNodeId`
- **integrity（完整性）**
  - `manifestSha256`：对 manifest（去掉本字段与signature）后的hash
  - `signature`：可选（平台签名，后续增强）

---

## 6. 验证机制（核心体验）

### 6.1 验证入口
- 在结果卡片/节点结果区域增加按钮：`验证作品`
- 也可在工具区提供“验证中心”页面

### 6.2 验证流程（离线可用）
1) 用户上传作品文件（mp4/png/txt）
2) 用户上传 `manifest.json`（或 zip）
3) 前端计算作品 sha256
4) 与 manifest 的 `outputs[].sha256` 对比
5) 输出结果：
- `匹配`：显示“验证通过：与证据包一致，未被篡改”
- `不匹配`：显示“验证失败：hash不一致，可能不是该版本或被编辑”

### 6.3 防篡改（MVP级）
- manifest 内存 `manifestSha256`
- 校验时先计算 manifest hash，确认未被修改

> 后续增强：平台对 manifest 进行签名（signature），验证端可校验公钥。

---

## 7. 隐私与安全设计

- **提示词保护**
  - 默认仅导出 `promptDigest`
  - 允许用户在导出时勾选“包含完整提示词”（弹窗提示风险）
  - 允许导出“加密提示词”（企业版增强）

- **用户身份保护**
  - 默认仅导出 `userIdHash`

- **合规边界文案**
  - 证据包证明的是“生成过程与版本一致性/未被篡改”，不直接等于法律层面的版权裁定

---

## 8. 产品交互与入口规划

### 8.1 UI入口（建议最小改动）
- **任务结果卡片**增加：
  - `下载证据包`
  - `验证作品`
- **项目页/画布页**增加：
  - `导出项目证据包`

### 8.2 导出选项（弹窗）
- 是否包含完整提示词（默认关闭）
- 是否包含输出文件本体（默认关闭/仅图片可选）
- 导出范围：当前结果 / 当前节点 / 整个项目

---

## 9. 商业化建议

### 9.1 免费版
- 单任务 Proof Pack（不含完整提示词、不含签名/时间戳）

### 9.2 Pro版
- 项目级 Proof Pack
- 批量导出
- 更长的证据留存

### 9.3 企业版
- 平台签名 + 可验证
- 第三方时间戳（按次收费）
- 团队审计（访问日志：谁下载过、何时验证过）

---

## 10. 开发里程碑（建议排期）

### 10.1 第1周（MVP上线）
- 生成 manifest.json（参数快照 + outputs hash）
- 下载导出（json或zip）

### 10.2 第2周（验证体验完善）
- 验证页面（纯前端hash对比）
- 项目级导出（画布快照 + 多输出）

### 10.3 第3-4周（增强与商业化）
- 平台签名（signature）
- 公钥验证
- 第三方时间戳（付费）

---

## 11. 成功指标（上线验证）

- **证据包导出率**：导出证据包的任务占比
- **验证使用率**：验证入口的使用次数
- **付费转化**：签名/时间戳/项目级导出升级的转化率
- **企业线索**：来自“交付/验真/审计”的咨询数量

---

## 12. 开放问题（需要在实施前确认）

- 输出文件hash计算位置：前端能否稳定获取文件二进制？还是需要后端对URL抓取并计算？
- 输出文件可能跨域/鉴权：hash计算如何处理（签名URL/临时token）
- 写作内容（文本）是否也纳入 Proof Pack（建议纳入）

---

## 13. 附：差异化卖点文案（对外一句话）

- “不仅能生成，更能验真：每个作品都带可验证的创作证据包。”
- “交付客户不再靠嘴说：一键生成可审计证据链。”
