# 角色库保存功能说明

## 📚 功能概述

在生成角色设定后，每个角色卡片都会显示 **"📥 存库"** 按钮，用户可以一键将角色保存到角色库中。

## 🎯 使用流程

### 1. 生成角色
- 启用"生成角色设定"选项
- 执行任务后，系统会自动生成角色设定图

### 2. 角色展示
每个生成的角色会显示为卡片，包含：
- **角色图片/视频**：点击可放大查看
- **角色名称**：如"李明"、"张伟"
- **角色描述**：简短的特征描述
- **操作按钮**：
  - `📥 存库` - 保存到角色库
  - `🎥 动效` - 生成角色动效视频（如未生成）

### 3. 保存到角色库
点击 `📥 存库` 按钮后：
- ✅ 角色信息（名称、描述、图片）会保存到本地存储
- ✅ 弹出提示："已保存到角色库"
- ✅ 可在右上角"📚 角色库和资产库"中查看

### 4. 查看已保存角色
1. 点击右上角 `📚 角色库和资产库` 按钮
2. 切换到 `角色库` 标签
3. 查看所有已保存的角色
4. 可对角色进行：
   - 🎥 生成动效视频
   - 🗑️ 删除角色

## 💾 数据存储

- **存储位置**：浏览器 LocalStorage
- **存储键名**：`character_library`
- **数据格式**：
```json
[
  {
    "name": "李明",
    "summary": "30岁男性，短黑发，穿着白色武道服...",
    "imageUrl": "https://...",
    "videoUrl": "https://..."  // 可选，生成动效后才有
  }
]
```

## 🎨 UI展示

### 角色卡片布局
```
┌─────────────────────────┐
│   [角色图片/视频]        │  ← 可点击放大
│   (160px高度)           │
├─────────────────────────┤
│ 【李明】                │  ← 角色名称
│ 30岁男性，短黑发...     │  ← 角色描述
│                         │
│ [📥 存库] [🎥 动效]     │  ← 操作按钮
└─────────────────────────┘
```

### 按钮状态
- **📥 存库**：始终显示，可重复保存（覆盖同名角色）
- **🎥 动效**：仅在未生成动效时显示，生成后自动隐藏

## 🔧 技术实现

### 保存函数
```javascript
window.saveCharacterToLibrary = (name, summary, imageUrl) => {
    let lib = JSON.parse(localStorage.getItem('character_library') || '[]');
    lib.push({ name, summary, imageUrl });
    localStorage.setItem('character_library', JSON.stringify(lib));
    alert('已保存到角色库');
};
```

### 调用方式
```html
<button onclick="saveCharacterToLibrary('李明', '30岁男性...', 'https://...')">
    📥 存库
</button>
```

## 🎯 适用场景

1. **角色复用**：常用角色一键存档，下次直接使用
2. **角色库管理**：统一管理所有生成的角色
3. **批量创作**：快速切换不同角色进行创作
4. **资产积累**：逐步积累自己的角色资产库

## 🔄 与角色库的关系

| 功能 | 当前任务 | 角色库 |
|------|---------|--------|
| 保存角色 | ✅ | ✅ |
| 查看角色 | ✅ | ✅ |
| 生成动效 | ✅ | ✅ |
| 删除角色 | ✅ | ✅ |
| 跨任务使用 | ❌ | ✅ |

## 📝 注意事项

1. **重复保存**：可以重复保存同名角色，不会自动去重
2. **数据持久化**：数据存储在浏览器本地，清空缓存会丢失
3. **图片依赖**：保存的是图片URL，源站删除则无法显示
4. **容量限制**：LocalStorage 有 5-10MB 的限制

## 🚀 未来优化方向

- [ ] 角色去重：同名角色自动覆盖或提示
- [ ] 云端同步：支持账号登录，角色云端保存
- [ ] 批量导出：一键导出所有角色为JSON文件
- [ ] 角色搜索：支持按名称、描述搜索角色
- [ ] 角色标签：为角色添加标签，方便分类管理

---

**版本**：V7.9.1+  
**最后更新**：2025-11-23

