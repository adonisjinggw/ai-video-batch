# 🎬 AI视频批量创作平台 V2.0 - 设计方案

## 📐 整体布局（参考剧本大师）

```
┌──────────────────────────────────────────────────────────┐
│  顶部：Logo + 状态 + 配置按钮                               │
├────────┬──────────────────────────────────────────────────┤
│        │                                                  │
│        │         无限画布工作区                            │
│  左侧  │     （显示任务卡片 + 实时进度）                   │
│  控制  │                                                  │
│  面板  │                                                  │
│        │                                                  │
│  模式  │                                                  │
│  设置  │                                                  │
│  任务  │                                                  │
│  列表  │                                                  │
│        │                                                  │
├────────┴──────────────────────────────────────────────────┤
│  底部：输入区 + 一键启动                                    │
└──────────────────────────────────────────────────────────┘
```

---

## 🎯 四种自动化模式

### **1. 手动模式** 🖐️
**流程**：用户输入 → 手动点击"生成剧本" → 手动点击"生成图片" → 手动点击"生成视频"

**适用场景**：
- 需要精细控制每一步
- 需要手动修改中间结果
- 测试API配置

**步骤显示**：
```
① 输入创意 ⏸️
② 生成剧本 [点击执行]
③ 生成图片 [点击执行]
④ 生成视频 [点击执行]
⑤ 完成 ✅
```

---

### **2. 半自动模式** ⚡
**流程**：用户输入 → 自动生成剧本 → **等待用户确认** → 自动生成图片/视频

**适用场景**：
- 需要审核剧本质量
- 可能需要修改剧本
- 想节省一些手动步骤

**步骤显示**：
```
① 输入创意 ⏸️
② 自动生成剧本 🔄
③ 等待用户确认剧本 ⏸️ [确认 / 修改]
④ 自动生成图片 🔄
⑤ 自动生成视频 🔄
⑥ 完成 ✅
```

---

### **3. 半全自动模式** 🚀
**流程**：用户输入 → 自动生成剧本+图片 → **等待用户确认** → 自动生成视频

**适用场景**：
- 快速预览图片效果
- 视频生成成本高，需要确认后再生成
- 适合大批量任务

**步骤显示**：
```
① 输入创意 ⏸️
② 自动生成剧本 🔄
③ 自动生成图片 🔄
④ 等待用户确认图片 ⏸️ [确认 / 重新生成]
⑤ 自动生成视频 🔄
⑥ 完成 ✅
```

---

### **4. 全自动模式** 🤖
**流程**：用户输入 → 一键到底，自动完成所有步骤

**适用场景**：
- 完全信任AI生成质量
- 大批量无人值守生成
- API配置已测试稳定

**步骤显示**：
```
① 输入创意 ⏸️
② 自动生成剧本 🔄
③ 自动生成图片 🔄
④ 自动生成视频 🔄
⑤ 完成 ✅
```

---

## 🎨 左侧控制面板（固定）

### **区域1：模式选择** （顶部）
```
┌─────────────────────┐
│  🎮 自动化模式      │
│  ○ 手动             │
│  ○ 半自动           │
│  ○ 半全自动         │
│  ● 全自动 （选中）   │
└─────────────────────┘
```

### **区域2：任务配置**
```
┌─────────────────────┐
│  📦 任务数量        │
│  [ 3 ] 个任务       │
│  ( - )  ( + )       │
│                     │
│  ⏱️ 每个任务时长    │
│  [ 15 ] 秒          │
│                     │
│  🎬 分镜数量        │
│  [ 12 ] 个镜头      │
└─────────────────────┘
```

### **区域3：任务列表**（可滚动）
```
┌─────────────────────┐
│  📋 任务列表 (3/10) │
├─────────────────────┤
│  ✅ 任务1: 侦探故事 │
│  🔄 任务2: 科幻冒险 │
│  ⏸️ 任务3: 爱情喜剧 │
└─────────────────────┘
```

---

## 🖼️ 右侧无限画布（工作区）

### **任务卡片设计**（类似剧本大师的场景卡片）

```
┌────────────────────────────────┐
│  🎬 任务1: 侦探故事              │
│  状态: 🔄 生成中 (步骤 2/4)     │
├────────────────────────────────┤
│  ① 输入创意 ✅                  │
│  ② 生成剧本 🔄 (30%)            │
│  ③ 生成图片 ⏸️                  │
│  ④ 生成视频 ⏸️                  │
├────────────────────────────────┤
│  📝 剧本预览：                  │
│  "一个夜晚的城市..."            │
│                                │
│  🎨 图片: [空]                  │
│  🎬 视频: [空]                  │
├────────────────────────────────┤
│  [⏸️ 暂停] [🔄 重试] [❌ 删除]  │
└────────────────────────────────┘
```

---

## ⚙️ 技术实现要点

### **1. 状态管理**
```javascript
const taskState = {
    mode: 'full-auto',      // 模式
    maxTasks: 10,           // 最大任务数
    tasks: [
        {
            id: 'task-1',
            title: '侦探故事',
            status: 'running',  // pending/running/paused/completed/failed
            step: 2,            // 当前步骤
            totalSteps: 4,      // 总步骤数
            progress: 30,       // 当前步骤进度%
            script: '',         // 生成的剧本
            images: [],         // 生成的图片
            videos: [],         // 生成的视频
            error: null         // 错误信息
        }
    ]
};
```

### **2. 模式执行流程**
```javascript
async function executeTask(task, mode) {
    switch(mode) {
        case 'manual':
            await waitForUserClick('generate-script');
            await generateScript(task);
            await waitForUserClick('generate-image');
            await generateImage(task);
            await waitForUserClick('generate-video');
            await generateVideo(task);
            break;
            
        case 'semi-auto':
            await generateScript(task);
            await waitForUserConfirmation('confirm-script');
            await generateImage(task);
            await generateVideo(task);
            break;
            
        case 'semi-full-auto':
            await generateScript(task);
            await generateImage(task);
            await waitForUserConfirmation('confirm-images');
            await generateVideo(task);
            break;
            
        case 'full-auto':
            await generateScript(task);
            await generateImage(task);
            await generateVideo(task);
            break;
    }
}
```

### **3. 实时进度更新**
```javascript
function updateTaskProgress(taskId, step, progress, message) {
    const taskCard = document.getElementById(`task-${taskId}`);
    taskCard.querySelector('.step-indicator').textContent = `${step}/${totalSteps}`;
    taskCard.querySelector('.progress-bar').style.width = `${progress}%`;
    taskCard.querySelector('.status-message').textContent = message;
    
    // 更新左侧任务列表
    updateSidebarTaskStatus(taskId, step, progress);
}
```

---

## 🎯 用户交互流程

### **启动任务**
```
1. 左侧选择模式
2. 设置任务数量
3. 底部输入创意（或批量上传）
4. 点击"🚀 开始批量生成"
5. 画布自动创建任务卡片
6. 实时显示每个任务的进度
```

### **任务控制**
```
- ⏸️ 暂停：暂停当前任务
- ▶️ 继续：继续暂停的任务
- 🔄 重试：重新执行失败的步骤
- ❌ 删除：删除任务卡片
- 👁️ 预览：查看生成结果
```

---

## 📊 进度指示器设计

### **全局进度（顶部）**
```
总进度: ████████░░ 80%  (8/10 完成)
```

### **单任务进度（卡片内）**
```
当前步骤: 生成图片 🔄
进度: ████░░░░░░ 40%
预计剩余: 2分钟
```

---

## 🎨 视觉设计原则

1. ✅ **参考剧本大师的深色科技风格**
2. ✅ **左侧固定控制面板（300px宽）**
3. ✅ **右侧无限画布（可缩放、拖拽）**
4. ✅ **任务卡片采用Material Design卡片风格**
5. ✅ **实时动画反馈（进度条、状态图标）**
6. ✅ **响应式设计（但优先桌面端）**

---

## 🔧 核心功能清单

- [x] 备份现有代码
- [ ] 创建新的HTML结构（参考storyboard-master）
- [ ] 实现左侧控制面板（模式选择+任务配置+任务列表）
- [ ] 实现右侧无限画布（类似storyboard-master的canvas）
- [ ] 实现4种自动化模式的执行逻辑
- [ ] 集成现有API（T8 Star文本、RH Flux图片、Sora2视频）
- [ ] 实现任务卡片组件（动态生成+实时更新）
- [ ] 实现进度显示系统（全局+单任务+步骤级）
- [ ] 添加任务控制按钮（暂停/继续/重试/删除）
- [ ] 实现画布交互（拖拽、缩放、重置视图）
- [ ] 保留现有的本地服务器proxy功能
- [ ] 添加错误处理和重试机制

---

## 🚀 下一步：开始实现

**优先级：**
1. ✅ 设计方案完成
2. ⏭️ 创建新的index.html（保留现有UI风格）
3. ⏭️ 实现核心状态管理
4. ⏭️ 实现4种模式的执行逻辑
5. ⏭️ 集成API调用
6. ⏭️ 完善UI动画和交互

**现在开始编码！** 🎉

