# RollRoll AI 创作平台 - 开发文档

## 📚 目录
- [项目概述](#项目概述)
- [技术架构](#技术架构)
- [功能模块](#功能模块)
- [开发优化记录](#开发优化记录)
- [Bug 修复记录](#bug-修复记录)
- [环境配置](#环境配置)
- [部署指南](#部署指南)

---

## 🎯 项目概述

**RollRoll** 是一个 Serverless AI 创作平台，基于 Vercel + Vanilla JS + Supabase 构建。用户可以输入创意主题，批量生成 AI 视频、图片、音乐、配音和文案。

- **官网**：https://lossloop.cn, https://www.rollroll.art
- **架构**：三层架构（浏览器前端 + Vercel Serverless + Supabase）
- **特点**：无构建步骤，纯 HTML/CSS/JS 直接托管

---

## 🏗️ 技术架构

### 三层架构
```
浏览器前端 (HTML/JS/CSS)
    ├── 页面层: index.html(PC), mobile.html(移动端), chat.html(AI对话), music.html(音乐)
    ├── 技能系统: skill-system.js + skill-presets.js
    ├── 团队系统: agent-team.js + agent-roles.js + agent-ui.js
    ├── API调用层: api-core.js, batch.js, billing.js
    └── 基础设施: resilient-api.js, task-orchestrator.js, supabase-config.js

Vercel Serverless Functions (/api)
    ├── sora2.js (视频), banana2.js (图片), yunwu.js (多模态)
    ├── writer-llm.js (LLM文本), modelscope.js (免费图片)
    ├── supabase-proxy.js (数据库/计费/认证), proxy.js (通用代理)
    └── video-continuity.js (连续性视频), cron-billing.js (定时任务), suno.js (音乐)

Supabase (数据库 + 认证)
```

### 核心系统

#### 1. 技能系统 (Skill System)
- **核心文件**：`js/skill-system.js` (~49KB) + `js/skill-presets.js` (~132KB)
- **功能**：
  - SkillManager：全局单例，注册/执行/搜索技能，最大并发 3
  - SkillUI：技能面板 UI，参数收集，进度展示
  - 17 个预置技能
- **分类**：`video | image | content | audio | design | tool | automation`

#### 2. 多智能体团队 (Agent Team System)
- **核心文件**：`js/agent-team.js` + `js/agent-roles.js` + `js/agent-ui.js`
- **角色**：coordinator, copywriter, visual_artist, video_producer, brand_strategist, character_designer, social_media_mgr, translator

#### 3. 弹性 API 网关 (resilient-api.js)
- 多节点负载均衡
- 熔断机制（连续 3 次失败触发熔断，60s 后半开恢复）
- 智能降级（付费节点 → 免费节点自动切换）

#### 4. 任务调度器 (task-orchestrator.js)
- 优先级队列（VIP 优先）
- 智能重试（指数退避，最大 3 次）
- 断点续传（localStorage 持久化）
- 并发控制（默认最大 3）

---

## 🎨 功能模块

### 1. 视频生成 (batch.js)
- 文生视频、图生视频
- 分镜生成
- 视频片段合并
- 无限画布任务管理

### 2. 图片生成 (banana2.js, modelscope.js)
- 文生图、图生图
- 多模型支持（Banana2, ModelScope, Midjourney）

### 3. 音乐生成 (music.html, api/suno.js)
- 灵感模式、自定义模式、智能模式
- 纯音乐/带人声选择
- 风格标签自定义
- 批量生成

### 4. 配音生成 (voice.html)
- TTS 文本转语音
- 多种声音选择

### 5. 文案生成 (writer-llm.js)
- LLM 文本生成
- 剧本创作

### 6. 多智能体协作 (chat.html)
- Agent Team 协作
- 工具调用

---

## 🚀 开发优化记录

### 2026-04-12 - 音乐生成纯音乐模式优化
**问题**：选择「纯音乐」仍然生成有人声的音乐

**原因**：
- 即使设置了 `make_instrumental`，前端仍然传递歌词给 Suno API
- Suno API 收到歌词时可能忽略 `make_instrumental` 参数

**优化方案**：
1. **前端修改** (`music.html`)：
   - 纯音乐模式清空 `prompt` 和 `currentLyrics`
   - 纯音乐模式不传递 `vocal_gender`
   - 纯音乐模式必须有风格标签

2. **批量生成优化**：
   - 每个格子纯音乐时清空 `cellPrompt`
   - 智能结构只在非纯音乐模式应用

3. **API 调用优化**：
   - `generateSingleMusicAPI()` 纯音乐模式只传 `tags`
   - `generateMusicWithLyricsAPI()` 纯音乐模式不传歌词

**修改文件**：
- `music.html` (多处)

---

### 2025-12-12 - 视频流程稳定性优化
**优化内容**：
1. 分镜生成自动重试（指数退避）
2. 每个视频片段卡片展示失败原因与重试按钮
3. 新增「一键重试失败片段」「重试合成」按钮
4. 抽出单分镜生成器 `generateOneClipWithRetry`

**修改文件**：
- `js/batch.js`

---

### 2025-12-12 - 流程窗显示优化
**优化内容**：
- 任务窗点击后自动缩放 + 居中（fit-to-viewport）
- 拖拽/缩放结束后执行互斥排斥（push-on-drop）
- 流程列布局改为等分 Grid，禁止横向滚动

**修改文件**：
- `js/batch.js`
- `css/style.css`

---

### 2025-11-24 - V7.9.14 角色库 UI 现代化
**优化内容**：
- 彻底替换原生 prompt()，采用精美模态框
- 三种创建方式：AI 生成/本地上传/网络链接
- 可视化选择

**修改文件**：
- 角色库相关文件

---

### 2025-11-22 - v2.2.0 自动托管模式
**新增功能**：
- 批量任务自动处理，无并发限制（串行）
- 配额不足自动暂停
- 断点续传（页面关闭后可恢复）
- 控制台 API：`toggleAutoHosting()`, `startAutoHosting()`, `getAutoHostingStatus()`

**优化内容**：
- 角色生成：优先使用 Banana2（无限并发），RH 作为降级
- ffmpeg.wasm 加载优化
- Sora2 轮询增强（兼容 8 种 URL 提取路径）

---

## 🐛 Bug 修复记录

### 2026-04-12 - 网页版登录/欢迎页逻辑修复
**问题**：网页版登录逻辑又错了，要和手机版一样不跳过登录页面

**根本原因**：
- `index.html` 的欢迎页逻辑和 `mobile.html` 不一致
- 缺少 `skip_welcome=1` 参数处理
- 没有清理残留 CSS 的逻辑，可能导致浏览器缓存旧版出现白屏/黑屏

**修复方法**：
将 `index.html` 的欢迎页逻辑完全替换为和 `mobile.html` 一致：
1. 支持 `logged_in=1` 和 `skip_welcome=1` 两个参数跳过欢迎页
2. 添加清理残留 CSS 的逻辑（`cinemaIntroInjectedStyle`）
3. 确保主页面元素（`#welcomeScreen,.app-container`）正确显示
4. 无登录参数时强制显示开场动画和欢迎页

**修改文件**：
- `index.html` (1448-1480)

---

### 2026-04-12 - 音乐生成纯音乐模式 Bug
**问题**：选择「纯音乐」仍然生成有人声的音乐

**根本原因**：
- 前端即使勾选纯音乐，仍然传递歌词（`prompt`）给 Suno API
- Suno API 收到歌词时，即使设置 `make_instrumental=true`，也可能生成人声

**修复方法**：
1. 在 `generateMusic()` 函数中，纯音乐模式清空 `prompt` 和 `currentLyrics`
2. 在 `generateSingleMusicAPI()` 中，纯音乐模式只传 `tags`，不传 `prompt`
3. 在 `generateMusicWithLyricsAPI()` 中，纯音乐模式不传歌词
4. 纯音乐模式不传递 `vocal_gender` 参数

**修改文件**：
- `music.html` (1884-1912, 2764-2772, 2842-2867, 3374-3390)

---

### 2025-12-12 - Sora2 备用贞贞分支 500 错误
**问题**：api/sora2.js 备用贞贞分支的基地址变量缺失（`API_BASE_URL` 未定义）

**修复方法**：
- 补充定义 `API_BASE_URL` 变量

**修改文件**：
- `api/sora2.js`

---

### 2025-12-12 - 0 片段合并崩溃
**问题**：`mergeVideos` 在 0 片段时抛异常，导致整个流程崩溃

**修复方法**：
- 0 片段时不再抛异常，改为记录错误并保持 UI 可重试

**修改文件**：
- `js/batch.js`

---

### 2025-12-11 - 域名强制跳转问题
**问题**：`rollroll.art` 被强制跳转到 `lossloop.cn`

**修复方法**：
1. 移除前端域名强制跳转（`js/supabase-config.js`）
2. 放行 `rollroll.art` / `www.rollroll.art` 的 CORS（`api/proxy.js`）
3. 取消 Vercel Host 重定向（`vercel.json`）

**修改文件**：
- `js/supabase-config.js`
- `api/proxy.js`
- `vercel.json`

---

### 2025-11-24 - V7.9.12 Banana2 图像生成修复
**问题**：Banana2 图像生成使用错误模型

**修复方法**：
- 正确使用 `nano-banana-2-4k` 模型
- 模型名称标准化

**修改文件**：
- 相关 API 调用文件

---

## 🔧 环境配置

### 必需环境变量
```env
YUNMENG_API_KEY          # 主力 AI API key（必须）
YUNMENG_API_KEY_2/3      # 备用 key（可选，轮换负载均衡）
SUPABASE_SERVICE_KEY     # Supabase service role key（必须）
VIP_SECRET               # VIP码验证（proxy.js）
MODELSCOPE_API_KEY       # ModelScope（降级图片）
WRITER_MIMO_API_KEY      # MIMO LLM（写作）
RUNNINGHUB_API_KEY       # 视频高清放大
```

### Vercel 配置
- Function timeouts: 180s（大部分），300s（yunwu.js, cron-billing.js）
- Daily cron: `/api/cron-billing` at 03:00 UTC
- 所有 HTML/JS/CSS: `Cache-Control: no-cache`

---

## 📦 部署指南

### 本地开发
```powershell
npm run dev              # vercel dev
```

### 部署（PowerShell）
```powershell
$env:NODE_TLS_REJECT_UNAUTHORIZED="0"; vercel --prod

# 或使用 npm scripts
npm run deploy           # vercel --prod
npm run deploy:stable    # vercel deploy --prod --yes
npm run predeploy        # scripts/sync-version.js 同步版本号
```

### 注意事项
- **无构建步骤**：纯 HTML/CSS/JS 直接由 Vercel 托管
- **缓存破坏**：JS/CSS 引用带版本参数如 `?v=2.2&t=20260209b`，更新文件后需同步更新引用处的版本参数
- **语法检查**：`node -c "文件路径"`（无测试框架）

---

## 📋 开发规范

### 代码修改原则
- **最小化修改**：局部修正，保持现有结构，验证后再改
- 不乱改用户配置好的模型/格式字符
- 不能大量删除或改动代码，除非用户明确指示
- 出错后不能用错误的备份文件覆盖替换，等待用户命令
- 全部修好再部署，不要每次没修好就部署

### Supabase 集成规则
- 使用 `var _sbClient` 而不是 `let supabase`（避免和 CDN 全局变量冲突）
- 所有 AI 功能必须有扣费逻辑，必须登录才能使用

### 移动端规则
- 按钮同时支持 `click` 和 `touchend` 事件
- 使用 `force=mobile` 参数标识来源

---

## 📊 项目文件速查

| 文件 | 功能 |
|------|------|
| `mobile.html` | 移动端主页 |
| `index.html` | PC端主页（自动重定向到 mobile.html）|
| `chat.html` | AI对话页（Agent Team UI）|
| `music.html` | 音乐生成页面 |
| `js/skill-system.js` | SkillManager + SkillUI 核心 |
| `js/skill-presets.js` | 17个预置技能定义 |
| `js/api-core.js` | 全页面共用的 API 调用函数 |
| `js/batch.js` | PC端核心逻辑：视频生成工作流、无限画布 |
| `js/billing.js` | 客户端两阶段扣费 |
| `js/supabase-config.js` | Supabase 初始化、认证状态管理 |
| `api/suno.js` | Suno 音乐生成 API |
| `api/sora2.js` | 视频生成 API |
| `api/banana2.js` | 图片生成 API |

---

**文档版本**：v1.0  
**最后更新**：2026-04-12
